<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Four Athens - Membership" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Membership" %>">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
  <%= csrf_meta_tags %>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.12.2/css/bootstrap-select.min.css">

  <!-- Latest compiled and minified JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.12.2/js/bootstrap-select.min.js"></script>

</head>
<body data-spy="scroll" data-target="#page-nav" class="<%= params[:id] %>">
  <!-- ******HEADER****** -->
  <header id="header" class="header">
    <div class="container">
      <h1 class="logo">
        <%= link_to(image_tag('fourathens-logo.png', height: "50"), root_path) %>
      </h1><!--//logo-->
      <%= render 'layouts/navigation' %>
    </div><!--//container-->
  </header><!--//header-->
  <%= render 'layouts/messages' %>
  <%= yield %>

  <%= render 'layouts/footer' %>
  <%= render 'layouts/modals' %>
</body>
</html>
