<h1>Plans#index</h1>
<p>Find me in app/views/plans/index.html.erb</p>
<% @plans.each do |plan| %>

  <%= plan.name %>
  <%= plan.price %>/<%= plan.frequency %>

  Details:
  <% plan.plan_details.each do |pd| %>
    <%= pd.content %>
  <% end %>
<% end %>